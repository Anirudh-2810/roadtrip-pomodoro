# Legacy — Tk + single-file web (pre-Next.js)

Preserved from tag pre-next. Original entry points: ../index.html (web) + ../roadtrip.py (Tk). New Next.js app is at repo root. Legacy kept for Pages fallback and history.

